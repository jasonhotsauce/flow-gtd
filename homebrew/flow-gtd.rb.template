cask "flow-gtd" do
  version "{{VERSION}}"
  sha256 "{{SHA256}}"

  url "https://github.com/{{GITHUB_USER}}/{{GITHUB_REPO}}/releases/download/v#{version}/Flow-#{version}-macos-{{RELEASE_ARCH}}.zip"
  name "Flow GTD"
  desc "Local-first, AI-augmented GTD native macOS app"
  homepage "https://github.com/{{GITHUB_USER}}/{{GITHUB_REPO}}"

  depends_on macos: ">= :sonoma"
  depends_on arch: {{ARCH_REQUIREMENT}}

  app "Flow.app"

  uninstall quit: "com.flowgtd.native"
end
